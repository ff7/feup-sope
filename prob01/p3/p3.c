#include <stdio.h>
int main(void)
{
  printf("Hello !\n");
  return 2;
}

// Same function of the p2 but changed the return code to test the command: "echo $?"

// In the part c) of the question we can see that after executing the command "ls" the return value is 0 as it was a successful operation
